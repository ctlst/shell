# Generic session defaults. User override: ~/.config/ctlst/session.conf
[session]
compositor = sway
renderer = auto
# Reserve an app workspace while a slow application starts. Applied at login.
launch_timeout_seconds = 30
# Optional applications are launched separately; no implicit phone services.
